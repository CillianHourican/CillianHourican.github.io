// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://cillianhourican.com',
  // User site (cillianhourican.github.io) serves at root — no base path needed.
});
